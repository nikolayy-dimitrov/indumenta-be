export type TaxonID = number & {
    readonly brand: unique symbol;
};
export declare function createTaxonID(taxonId: number): TaxonID;
export type NormalizedBbox = [number, number, number, number];
export declare enum TaxonType {
    category = "category",
    trait = "trait"
}
export type TaxonPrediction = {
    id: TaxonID;
    type: TaxonType;
    name: string;
    displayName: string;
    score?: number;
    children: TaxonPrediction;
};
export declare const BASE_API_URL = "https://api.dragoneye.ai";
